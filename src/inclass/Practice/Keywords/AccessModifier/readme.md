# ACCESS MODIFIER 

1. Public [Everywhere from any class in any package]
2. Private [Only within the same class.][most restricted][encapsulation]
3. Default [Only within the same package]
4. Protected [Only within the same package AND bY subclasses][inheritance]