# COMPARE METHODS [ TO GET SORT ORDER OF WHEN THERE IS TWO METHOD ]
1. COMPARABLE 
2. COMPARATOR 

# COMPARABLE [class itself implements comparable<class> ]

# COMPARATOR  [MAKEs SEPARATE CLASS implements Comparator<class>]

# SOLID PRINCIPLE 
# MAP

1. HashMap
2. TreeMap
3. LinkedHashMap



# READ BY YOURSELF 
4. ConcurrentHashMap
5. IdentityHashMap
6. Synchronized HashMap

   /// Comparable	                                      Comparator
   /// Belongs to the object (class itself)	          Separate object for comparison
   /// Only one way to compare	                          Can have multiple comparison logics
   /// Uses compareTo()	                              Uses compare()


# KEY-VALUE PAIR = ENTRY 

# hashset is internally backed  by hashmap